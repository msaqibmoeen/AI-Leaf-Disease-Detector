# AI-Leaf-Disease-Detector
Built an AI Leaf Disease Detector web app for my AI course using a trained ML/DL model (not hard-coded). It identifies plant type, disease, category, severity, and confidence from leaf images and generates a diagnosis (symptoms, causes, treatment, prevention) with secure login, upload, and results dashboard.
